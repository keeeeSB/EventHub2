<h2 class="text-center mb-5">未開催イベントの一覧</h2>

<div class="text-center mb-4">
  <%= link_to "開催済みのイベントはこちら", past_user_events_path(@users), class: "btn btn-outline-secondary" %>
</div>

<div class="d-flex justify-content-center">
  <div class="col-md-6">
    <% if @upcoming_events.present? %>
      <% @upcoming_events.each do |event| %>
        <%= render "shared/event_card", event: event %>
      <% end %>
    <% else %>
      <div class="text-center">
        現在、開催予定のイベントはありません。
      </div>
    <% end %>
  </div>
</div>
